# Regenerate the lockfile (from repo root):
#   uv pip compile examples/docker_compose/requirements.in -o examples/docker_compose/requirements.txt
# If the requested FluxLit range is not on PyPI yet, build wheels first (`uv build -o /tmp/fluxlit-wheels`)
# and add `--find-links /tmp/fluxlit-wheels` to the compile command.
fluxlit>=0.13,<1.0
