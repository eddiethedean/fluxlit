# Same discovery as [tool.ty.src] in pyproject.toml, but without the tests/** rule
# overrides. Use from the repo root:
#   ty check --config-file ty.strict-tests.toml
[src]
include = ["src", "tests"]
