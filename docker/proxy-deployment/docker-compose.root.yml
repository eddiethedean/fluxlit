# Merge with docker-compose.yml: root-path proxy on 8082.
services:
  fluxlit:
    environment:
      FLUXLIT_ROOT_PATH: ""
  nginx:
    ports:
      - "8082:80"
    volumes:
      - ./nginx-root.conf:/etc/nginx/conf.d/default.conf:ro
